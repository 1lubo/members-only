<% if flash && !flash.empty? %>
    <% flash.each do |key, message| %>
        <% message.each do |m| %>
            <div class="alert alert-<%= flash_class(key) %>">            
                <h6><%= m %></h6>                 
            </div>
        <% end %>    
    <% end %>    
<% end %>
